"use client"

import { Card, CardHeader, CardContent, CardTitle } from "@/components/ui/card"

export function OrderHistory() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Order History & Reports</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Check completed orders and generate reports.</p>
      </CardContent>
    </Card>
  )
}
