"use client";

import { Card, CardHeader, CardContent, CardTitle } from "@/components/ui/card";

export function Inventory() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Inventory (Coming Soon)</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Inventory management will be available in the next phase.</p>
      </CardContent>
    </Card>
  );
}
