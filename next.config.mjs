/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com","img.freepik.com","foodish-api.com","res.cloudinary.com"],
  },
};

export default nextConfig;
